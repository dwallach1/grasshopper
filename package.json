{
  "name": "thesisforge",
  "version": "0.1.0",
  "private": true,
  "packageManager": "bun@1.4.0",
  "type": "module",
  "workspaces": ["apps/*", "workers/*", "packages/*"],
  "scripts": {
    "build": "bun run --cwd apps/dashboard build",
    "typecheck": "bun run --cwd workers/research typecheck && bun run --cwd workers/knowledge typecheck && bun run --cwd workers/broker typecheck",
    "test": "bun run --cwd workers/research test && bun run --cwd workers/knowledge test && bun run --cwd workers/broker test",
    "dry-run": "bun run --cwd apps/dashboard dry-run && bun run --cwd workers/research dry-run && bun run --cwd workers/knowledge dry-run && bun run --cwd workers/broker dry-run",
    "dev": "bun run --cwd apps/dashboard dev"
  }
}
