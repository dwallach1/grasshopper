{
  "name": "thesisforge",
  "version": "0.1.0",
  "private": true,
  "packageManager": "bun@1.4.0",
  "type": "module",
  "workspaces": ["apps/*", "workers/*", "packages/*", "tests/*"],
  "scripts": {
    "build": "bun run --cwd apps/dashboard build",
    "typecheck": "bun run --cwd packages/shared typecheck && bun run --cwd workers/research typecheck && bun run --cwd workers/knowledge typecheck && bun run --cwd workers/broker typecheck",
    "test": "bun run --cwd packages/shared test && bun run --cwd workers/research test && bun run --cwd workers/knowledge test && bun run --cwd workers/broker test",
    "test:e2e": "bun run --cwd tests/e2e test",
    "dry-run": "bun run --cwd apps/dashboard dry-run && bun run --cwd workers/research dry-run && bun run --cwd workers/knowledge dry-run && bun run --cwd workers/broker dry-run",
    "cloud:sync": "bun run --cwd workers/knowledge sync",
    "cloud:trigger": "bun run --cwd workers/research trigger",
    "local:setup": "bash scripts/local-dev-setup.sh",
    "local:web": "bun run --cwd apps/dashboard dev",
    "dev": "bun run local:web"
  },
  "devDependencies": {
    "@oxlint/plugins": "^1.80.0"
  }
}
