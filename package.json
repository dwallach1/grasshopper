{
  "name": "thesisforge",
  "version": "0.1.0",
  "private": true,
  "packageManager": "bun@1.4.0",
  "type": "module",
  "scripts": {
    "build": "bun run --cwd web build",
    "typecheck": "bun run --cwd web workflow:typecheck && bun run --cwd web knowledge:typecheck && bun run --cwd web broker:typecheck",
    "test": "bun run --cwd web workflow:test && bun run --cwd web knowledge:test && bun run --cwd web broker:test",
    "dry-run": "bun run --cwd web research:dry-run && bun run --cwd web knowledge:dry-run && bun run --cwd web broker:dry-run"
  }
}
