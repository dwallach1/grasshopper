import { StewardExpressionSheet } from '../terminal/steward-expression-sheet';

export const metadata = {
  title: 'Steward expression family',
};

export default function StewardSheetPage() {
  return <StewardExpressionSheet />;
}
